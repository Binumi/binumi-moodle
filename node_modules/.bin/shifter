#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../shifter/bin/shifter" "$@"
  ret=$?
else 
  node  "$basedir/../shifter/bin/shifter" "$@"
  ret=$?
fi
exit $ret
